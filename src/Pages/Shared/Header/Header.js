import React from 'react';
import NavBar from '../../Home/NavBar/NavBar';

const Header = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default Header;